typedef enum  
{
	fr_all,
	all,
	common,
	nyt,
} ALL_WORD_LISTS ;

typedef enum 
{
	draw,
	strict,
	xdraw,
} warnings;

typedef enum
{
	drawing_mode,
	matching_mode,
	validating_mode,
} parsing_modes;
